import { CalculateOriginalPricePipe } from './calculate-original-price.pipe';

describe('CalculateOriginalPricePipe', () => {
  it('create an instance', () => {
    const pipe = new CalculateOriginalPricePipe();
    expect(pipe).toBeTruthy();
  });
});
